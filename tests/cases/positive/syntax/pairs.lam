-- non-dependent tensor
t : <A * B>

-- tensor with bound variable
t : <x : A * B>

-- tensor with graded binder
t : <x [.0] : A * B>

-- pair
t = <x, y>

-- tensor eliminator
t = case p of <x, y> -> t

-- tensor eliminator (with as)
t = case p as z in C of <x, y> -> t
